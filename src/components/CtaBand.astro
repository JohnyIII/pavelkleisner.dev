---
const {
    title,
    body,
    href,
    label,
} = Astro.props;
---

<section class="cta-band">
    <div>
        <h2>{title}</h2>
        {body && <p>{body}</p>}
    </div>
    {href && label && <a href={href} class="btn-primary">{label}</a>}
</section>
